//! Riffle library: shared modules for both the dashboard binary (`riffle`)
//! and the standalone sink CLI (`riffle-sink`).

pub mod config;
pub mod consumer;
pub mod producer;
pub mod sample_data;
pub mod sink;
pub mod state;
pub mod web;
